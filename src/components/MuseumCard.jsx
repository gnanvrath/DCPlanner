import React from 'react';\nimport { addToPlanner } from './plannerStorage';\n\nexport default function MuseumCard({ museum }) {\n  const handleAdd = () => {\n    addToPlanner(museum);\n    window.dispatchEvent(new Event('planner:update'));\n  };\n\n  return (\n    <article className="card museum-card">\n      <div style={{display:'flex',justifyContent:'space-between',alignItems:'start',gap:12}}>\n        <div style={{flex:1}}>\n          <h3>{museum.name}</h3>\n          <div className="meta">{museum.organization} — {museum.address}</div>\n        </div>\n        <div style={{textAlign:'right'}}>\n          <div style={{fontWeight:700}}>{museum.free ? 'Free' : 'Paid'}</div>\n          <div className="meta">{museum.booking_required ? 'Booking required' : 'Walk-ins OK'}</div>\n        </div>\n      </div>\n\n      <p style={{margin:0,color:'#333'}}>{museum.description}</p>\n\n      <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginTop:8}}>\n        <div style={{display:'flex',gap:8}}>\n          {museum.website && <a href={museum.website} target="_blank" rel="noreferrer" className="small-btn">Website</a>}\n          <a className="small-btn" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(museum.address)}`} target="_blank" rel="noreferrer">Map</a>\n        </div>\n\n        <div>\n          <button className="btn" onClick={handleAdd}>Add to plan</button>\n        </div>\n      </div>\n    </article>\n  );\n}