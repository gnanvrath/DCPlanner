// simple localStorage-backed planner utilities\nconst KEY = 'dcplanner:plan';\n\nexport function getPlanner() {\n  try {\n    const raw = localStorage.getItem(KEY);\n    if (!raw) return [];\n    return JSON.parse(raw);\n  } catch (e) {\n    return [];\n  }\n}\n\nexport function addToPlanner(museum) {\n  const items = getPlanner();\n  if (items.find(i => i.id === museum.id)) return;\n  items.push({ id: museum.id, name: museum.name, organization: museum.organization });\n  localStorage.setItem(KEY, JSON.stringify(items));\n}\n\nexport function removeFromPlanner(id) {\n  const items = getPlanner().filter(i => i.id !== id);\n  localStorage.setItem(KEY, JSON.stringify(items));\n}\n\nexport function clearPlanner() {\n  localStorage.removeItem(KEY);\n}