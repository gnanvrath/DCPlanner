import React from 'react';\n\nconst categories = ['All', 'Art', 'Science', 'History', 'Other'];\n\nexport default function Filters({ filters, setFilters, organizations }) {\n  const update = (patch) => setFilters(f => ({ ...f, ...patch }));\n\n  return (\n    <div className="card">\n      <div className="filter-group">\n        <label>Search</label>\n        <input className="search-input" value={filters.query} onChange={e => update({ query: e.target.value })} placeholder="Search museums, tags, descriptions..." />\n      </div>\n\n      <div className="filter-group">\n        <label>Category</label>\n        <div className="filter-row">\n          {categories.map(cat => (\n            <button key={cat} className={`small-btn ${filters.category===cat ? 'btn' : 'btn secondary'}`} onClick={() => update({ category: cat })}>\n              {cat}\n            </button>\n          ))}\n        </div>\n      </div>\n\n      <div className="filter-group">\n        <label>Organization</label>\n        <select value={filters.organization} onChange={e => update({ organization: e.target.value })} style={{width:'100%',padding:'8px',borderRadius:8}} >\n          <option>All</option>\n          {organizations.map(org => <option key={org} value={org}>{org}</option>)}\n        </select>\n      </div>\n\n      <div className="filter-group">\n        <label>Options</label>\n        <div style={{display:'flex',flexDirection:'column',gap:8}}>\n          <label><input type="checkbox" checked={filters.freeOnly} onChange={e => update({ freeOnly: e.target.checked })} /> Show free only</label>\n          <label><input type="checkbox" checked={filters.bookingRequired} onChange={e => update({ bookingRequired: e.target.checked })} /> Booking required</label>\n        </div>\n      </div>\n    </div>\n  );\n}